# Postgres connection string (Neon now; later a local DB via Cloudflare Tunnel)
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require

# Auth.js v5 — generate with: openssl rand -base64 32
AUTH_SECRET=

# Required for Auth.js v5 behind proxies / non-localhost hosts
AUTH_TRUST_HOST=true
